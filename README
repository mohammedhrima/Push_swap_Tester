open push_swap directory and copy past the following commands

git clone https://github.com/mohammedhrima/Push_swap_Tester tester

make tests -C ./tester

// for slow tests execution in case you want to debug your code
make debug -C ./tester   

